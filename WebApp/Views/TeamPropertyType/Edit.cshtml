@model WebApp.Models.TeamPropertyType

@{
    ViewBag.Title = "Редактировать тип атрибута команды";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("Edit", "TeamPropertyType", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.ValidationSummary()
    @Html.HiddenFor(x => x.Id)
    @Html.EditorForModel()

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" class="btn btn-default" value="Редактировать" />
        </div>
    </div>
}